const { Coordinate, CoordinatePropType } = require('./Coordinate');
const { TimeSlice, TimeSlicePropType } = require('./TimeSlice');
const { TravelPath, TravelPathPropType } = require('./TravelPath');

module.exports = {
  Coordinate,
  CoordinatePropType,
  TimeSlice,
  TimeSlicePropType,
  TravelPath,
  TravelPathPropType,
};
